---
id: issues-154
title: "Update README with correct information"
description: "Audit and correct root README.md so status, install, quickstart, and feature claims match the product."
status: reviewing
issue-type: observation
severity: low
tags:
  - planning
  - issue
  - docs
  - ready-for-agent
---

# Update README with correct information

## Description

Root [`README.md`](../../../README.md) should reflect current product truth: version/status line, install path, quickstart commands, shipped vs sketch surfaces, and links. Audit against CLI help, `CHANGELOG.md`, `docs/reference/*`, and examples; fix stale or wrong claims.

## Affected

- `README.md`
- Possibly cross-links into `docs/reference/` and `examples/*` if README points at moved/renamed paths

## Observed

Triage audit (2026-08-02): **no material wrongs**. Prior honesty work ([[issues-54-readme-post-010-docs-drift]], [[issues-76-readme-slot-depth-honesty]]) already landed. Residual gaps are completeness nits only (see Agent Brief).

## Impact

First-touch docs mislead humans and agents; wrong install/quickstart burns time. Current risk is low — polish, not crisis.

## Proposed Fix

1. Apply the small completeness nits in the Agent Brief
2. Keep honesty on sketch vs shipped (no aspirational commands as if live)
3. Do not expand README into a full CLI reference

## Agent Brief

**Category:** enhancement
**Summary:** Polish root README completeness so worktree/`generate` flags match shipped CLI; no rewrite.

**Current behavior:**
Root README already matches product truth for v0.1.0: status line (spine + worktrees/generate; agent pack local v1; `agent prompt` thin; `agent start` sketch; doctor orphan/dirty + pack_missing), install (source primary; Releases “when published”), quickstart commands, sketch honesty, and working doc/example links. Prior closed honesty issues fixed the large post-0.1.0 drift.

**Desired behavior:**
README remains honest and gains three optional completeness touches so readers do not need reference docs for common shipped flags:

1. Worktree surface names **add / list / rm / prune** (status blurb and/or quickstart), matching `odm project worktree --help`.
2. Generate example includes `[--force]` alongside `[--dry-run]`.
3. Optional: core-desk dogfood block mentions the existing full-tour script under that example’s `scripts/` (manual steps may stay).

Preserve: sketch labeling for `agent start` and graph; Releases gated by “when published”; brevity (not a full cli.md mirror).

**Key interfaces:**
- Root README status one-liner and quickstart command blocks
- Product truth sources for verification: `odm --help` / subcommand help, crate version `0.1.0`, CHANGELOG, reference CLI/install docs

**Acceptance criteria:**
- [ ] Status and/or quickstart mention worktree **rm** alongside add/list/prune
- [ ] Generate example documents `[--force]` (and still documents `--dest` / `[--dry-run]`)
- [ ] Optional: core-desk section points at `scripts/dogfood.sh` (or equivalent path if renamed — discover via example README)
- [ ] No new false claims: `agent start` stays sketch/not-implemented; no invented commands
- [ ] All README links still resolve
- [ ] `odm --version` / crate version still agree with any version wording touched

**Out of scope:**
- Website copy ([[issues-155-align-website-current-project]])
- Full CLI reference expansion, feature matrix redesign, pack quickstart block
- Version bump or release publishing
- Changing CLI behavior

## Comments

Filed from request to track README accuracy; triage should list concrete wrong lines before `ready-for-agent`.

### 2026-08-02 triage

> *This was generated by AI during triage.*

- **Category:** enhancement (docs honesty); kept `issue-type: observation`
- **State:** `needs-triage` → `ready-for-agent`
- **Redundancy check:** searched prior README honesty work — closed #54 / #76 already fixed material drift; fresh CLI/`--help`/CHANGELOG pass found no false claims
- **Prior rejection:** no matching `.out-of-scope/` entry
- **Verify:** confirmed README matches `odm 0.1.0` and shipped surfaces; residual = completeness nits only
- Severity lowered medium → low
