---
id: issues-164
title: "Update docs and website"
description: "Audit and refresh reference docs plus website/ so claims match current product truth after recent delivery."
status: closed
issue-type: observation
severity: medium
tags:
  - planning
  - issue
  - docs
  - website
---

# Update docs and website

## Description

Audit and update **reference docs** under `docs/reference/` and the public site under `website/` so status, install, quickstart, CLI surface, concepts/features/guides, and honesty (shipped vs sketch) match the product as of current main.

Prior honesty passes already closed:

- [[issues-154-update-readme-correct-info]] — root README polish
- [[issues-155-align-website-current-project]] — website residual copy drift
- [[issues-163-agent-start-docs-dogfood]] — promote `agent start` from sketch in docs/README/website + dogfood

This issue is a **fresh full pass** after that wave (and any commits since), not a re-open of those tickets. Fix stale, wrong, or aspirational-as-shipped claims; fill material omissions; keep sketch labeling honest.

## Affected

- `docs/reference/*` (especially `cli.md`, `install.md`, `env-gen-packs.md`, `architecture.md`, `phased-delivery.md`, guides-adjacent reference)
- `website/` public HTML (index, install, quickstart, cli, config, concepts, features, guides, `guide-*.html`)
- Possibly `website/e2e` if copy changes break assertions
- Cross-links from site ↔ reference ↔ root README (README-only polish stays out of scope unless a link is broken)

## Observed

Triage spot-check (2026-08-02): `odm agent start` is correctly **v1** on live help, README status, `docs/reference/cli.md` / `env-gen-packs.md`, and website features/cli/guide-agents. Install still correctly says build-from-source primary and Releases “when published” (binary publish is [[issues-165-multiplatform-github-releases-curl-install]], not this ticket). Residual risk is thin wording drift, dead links, or omissions vs full CLI surface — not a known large false-sketch on `agent start`.

## Impact

Public and in-repo docs mislead humans and agents when they lag shipped CLI.

## Proposed Fix

See Agent Brief.

## Agent Brief

> *This was generated by AI during triage.*

**Category:** enhancement  
**Summary:** Full residual honesty pass on `docs/reference/` + `website/` against live CLI and status surfaces; fix material defects or close clean.

**Current behavior:**

- Targeted honesty tickets 154 / 155 / 163 are closed; product includes worktree slots, local generate (+ dry-run), agent packs, thin `agent prompt`, and `agent start` v1 one-shot.
- Crate version **0.1.0**; CHANGELOG large `[Unreleased]` with rolling note (see closed [[issues-157-release-hygiene-rolling-unreleased]]).
- Install docs/site still correctly treat GitHub Releases as not-yet-published; do **not** invent curl/binary install here.

**Desired behavior:**

1. **Audit** against live truth: `odm --help` and major subcommand help, crate version, `CHANGELOG.md` Unreleased themes, root README status line, examples READMEs if linked from site/docs.
2. **Fix** only material wrong/stale/aspirational-as-shipped claims and broken internal links. Preserve sketch vs shipped badges/wording for true deferred items (remote generate, marketplace, serve/MCP, interactive init, agent-start depth beyond one-shot, Windows primary install, etc.).
3. **Website e2e** — if any `website/*.html` copy changes, run Playwright smoke (`website/` package scripts) and fix assertions if needed.
4. **Close** with a short Comments note: either bullet list of what changed, or “no material drift” if audit is clean. Do not expand into redesign or CLI behavior changes.
5. **Coordinate with #165** — install “when published” / build-from-source primary stays correct until binary releases ship; leave curl/install.sh work to [[issues-165-multiplatform-github-releases-curl-install]].

**Key interfaces:**

- Honesty surfaces: root README status; website index status chip + features shipped/sketch; `docs/reference/cli.md` full-vs-sketch matrix; `env-gen-packs.md`; `install.md` / `website/install.html`.
- Live CLI: top-level commands and `agent` / `generate` / `project worktree` subcommands.
- Verification: `website` Playwright e2e when HTML changes.

**Acceptance criteria:**

- [x] Audit recorded (Comments or brief checklist) against live help + README + CHANGELOG themes
- [x] No remaining material false “sketch” or false “shipped” claims on landed surfaces (especially agent start, generate dry-run, worktree prune, pack list missing, doctor warns)
- [x] Internal site/docs links that the pass touches still resolve; e2e green if HTML changed
- [x] Install copy still honest about Releases until #165 delivers binaries (no fake curl primary)
- [x] Issue closed or left open only if blocked on human copy judgment (prefer close)

**Out of scope:**

- Root README-only polish already covered by 154 (unless a cross-link is broken)
- Implementing binary Releases / `install.sh` (#165)
- Website visual redesign, new pages, coverage CI
- Changing CLI behavior or exit/JSON contracts

## Comments

### Filed

Filed from request to update docs and website.

### 2026-08-02 triage

> *This was generated by AI during triage.*

- Category: enhancement (`observation`)
- State: `needs-triage` → `ready-for-agent`
- Redundancy: not a re-open of closed 154 / 155 / 163; those were targeted; this is residual full pass
- Prior rejection: `.out-of-scope/` empty
- Spot-check: agent start honesty OK; install “when published” still true
- Agent Brief attached

### 2026-08-02 close (issues-164)

**Audit** (live `./target/debug/odm` 0.1.0 + README status + CHANGELOG Unreleased):

- Top-level: `init`, `sync`, `pin`, `status`, `doctor`, `project`, `progen`, `find`, `context`, `run`, `generate`, `agent` — matches `cli.md` / website `cli.html`
- `agent pack` list/install/link/rm; `agent prompt`; `agent start` one-shot — shipped, not sketch
- `generate --dry-run` / `--force`; `project worktree` list/add/rm/prune/prune --all — shipped
- Doctor warns (orphan/dirty/pack_missing) + status `agent_packs` / pack list `missing` — documented
- Install: build-from-source primary; Releases “when published” (no curl primary) — leave #165
- Deferred correctly: `init --interactive`, remote generate, marketplace, serve/MCP, start depth beyond one-shot
- Website internal links: 0 broken; Playwright e2e 33/33 green after HTML edits

**Changes (material residual only):**

- `docs/reference/cli.md` + `worktrees.md` — `--wt` path binding includes `agent start` (was only git/run)
- `docs/reference/cli.md` related + `config.md` deferred — `env-gen-packs` mixed depth, not pure sketch
- `website/features.html`, `guide-worktrees.html`, `guide-agents.html` — start + `--wt` honesty
- `CHANGELOG.md` Unreleased Fixed note

No redesign, no CLI behavior change, no install.sh / Releases (#165).
