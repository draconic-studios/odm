---
id: issues-151
title: "Split membership module under ≤1000 LOC"
description: "Decompose membership grab-bag (add/rm, project_git, path helper, massive tests) so no file sits on the 1k wall."
status: closed
issue-type: feature-request
severity: high
tags:
  - planning
  - issue
  - wayfinder
  - wayfinder-task
  - architecture
  - deepen
---

# Split membership module under ≤1000 LOC

## Description

The membership module is at ~993 LOC (production + large inline tests): entity add/rm, path-policy error remap, delete-with-restore, gitignore/pin hooks, `project_git` passthrough, and a CLI-oriented path helper. Next feature growth crosses the AGENTS.md ≤1000 target without a structural split. Same spirit as the prior worktree test extract.

## Affected

- odm-core membership and related re-exports
- Callers of membership add/rm and project git (CLI)
- Unit tests currently colocated in the membership module

## Impact

Harder reviews; forced LOC debt; divergent change on one file for unrelated reasons.

## Proposed Fix

See Agent Brief.

## Blocked by

None

## Agent Brief

> *This was generated by AI during triage.*

**Category:** refactor  
**Summary:** Split the membership grab-bag so every resulting `.rs` file is ≤1000 LOC (prefer production modules lean); no product behavior change.

**Current behavior:**

- One module owns: project/progen membership add and rm (including path escape validation before save, unique path-safe names), delete-with-restore, gitignore/pin side effects, `project_git` with worktree/pin-maintain branching, and a path buffer → rel helper used for CLI, plus a large ScriptedRunner-style unit test bulk.

**Desired behavior:**

- Clear ownership boundaries, for example (names flexible):
  - Membership add/rm (+ path validation hooks they need)
  - Project git passthrough / pin maintain at git boundary
  - Path helper lives with path policy or bin-side if it is purely presentation
  - Tests in sibling `*_tests.rs` via `#[cfg(test)] #[path = …]` and/or shared testsupport for ScriptedRunner/fixtures used by multiple core modules
- Public API used by CLI remains available from the core library façade (re-exports OK) with the same signatures and semantics.
- Prefer extracting shared git test doubles once if multiple modules already paste ScriptedRunner — only as needed to keep files under target; do not build a speculative test framework.
- No CHANGELOG product entry required (pure refactor); module comments OK.

**Key interfaces:**

- Existing membership add/rm and project git functions and error codes (usage vs not_found vs operation) — unchanged
- Path token / resolve_under_root policy — do not weaken; typed path errors may land in [[issues-153-typed-path-resolve-errors]] separately (string remap may remain until then)

**Acceptance criteria:**

- [x] No single membership-related production or test `.rs` file exceeds 1000 LOC; none exceed 1250
- [x] Former monolith file (if kept) is ≤1000 LOC
- [x] Public behavior of add/rm/project git unchanged (existing unit + CLI tests pass)
- [x] `lib` re-exports or paths still compile for current CLI callers without behavior change
- [x] `cargo test` green; `cargo clippy --workspace --all-targets -- -D warnings` clean

**Out of scope:**

- New membership features
- Inventory / CLI spine / fsutil work
- Changing path error typing (unless a tiny internal move with no behavior change)
- Docs beyond one-line module comments

## Acceptance

- [x] Agent Brief acceptance criteria all met

## Answer

Split membership grab-bag by ownership:

- `membership.rs` (~313 LOC) — add/rm + thin project/progen wrappers
- `project_git.rs` (~83 LOC) — git passthrough / pin maintain on primary
- `path_buf_to_rel` moved to `paths.rs` (path policy)
- Tests: `membership_tests.rs` (~329), `project_git_tests.rs` (~283) via `#[cfg(test)] #[path = …]`

Crate-root re-exports unchanged for CLI. `cargo test` green; clippy `-D warnings` clean.

## Comments

From thermo-nuclear review 2026-08-02; parent map [[issues-148-thermo-nuclear-structure-map]]. Pattern reference [[issues-83-worktree-module-split]].
