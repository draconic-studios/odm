---
id: issues-29
title: "Progen slice order and acceptance"
description: "Decision: vertical slice order and phase-3 map-close checklist."
status: closed
issue-type: feature-request
tags:
  - planning
  - issue
  - wayfinder
  - wayfinder-grilling
---

# Progen slice order and acceptance

## Question

What is the vertical implement order for phase 3, and when is the Progen integration map closable?

## Blocked by

- [[issues-28-progen-engine-and-obsidian-contract]]

## Answer

**Slice order** (vertical; unbuilt store verbs exit usage “not implemented”):

1. `odm-progen` crate + scope resolution (`--progen` / `--progen-group` / default-all; write single-Progen rule)
2. Progen lifecycle CLI: `list` / `add` / `rm` / `info` (mirror project; vault scaffold on local add)
3. Index under `.odm/progen/<name>/` + `progen reindex` + `progen ls` / `progen get`
4. Federated `odm find` + single-store `odm context` (wikilink neighborhood; `name:id` disambiguation)
5. `odm progen doctor` (path present, index openable/fresh) — ODM `doctor` stays ODM-side only
6. Dogfood vault in example Workspace + integration tests gate

**Map-close checklist (phase gate):**

- [x] `odm-progen` in workspace; bin routes store verbs through it
- [x] Lifecycle + scope flags match `progen.md` / `cli.md`
- [x] Indexes only under `.odm/progen/<name>/`
- [x] `find` / `context` honor scope; JSON tags `"progen"`
- [x] Local vault is plain Markdown; Obsidian-openable (no required engine files in vault)
- [x] `cargo test` green including progen integration scenarios

## Comments

> *This was generated by AI during triage.*
