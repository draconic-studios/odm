---
id: issues-159
title: "Post-drain dogfood / QA gap filing"
description: "Run core-desk dogfood + full_tour gate; file real product/docs bugs as ready-for-agent issues; close with no new gaps if clean. Agent-only."
status: open
issue-type: observation
severity: low
tags:
  - planning
  - issue
  - wayfinder
  - wayfinder-task
  - qa
  - dogfood
  - ready-for-agent
---

# Post-drain dogfood / QA gap filing

## Description

After empty frontier drain (154/155) and parent [[issues-156-post-drain-next-wave-map]], re-run the full capability verification tools and file residual bugs. Same pattern as closed [[issues-147-demo-gap-followups]]. Agent-only — no human `/qa` required.

## Affected

- Product crates / docs as discovered
- This issue’s Comments log
- Newly filed gap issues (if any)

## Proposed Fix

See Agent Brief.

## Blocked by

None — parent AFK defaults recorded; dogfood does not wait on release cut or product map.

## Agent Brief

> *This was generated by AI during triage / map minting.*

**Category:** chore  
**Summary:** Run full dogfood + tour gate; for each unexpected failure or honesty gap, create a new `ready-for-agent` issue under `docs/planning/issues` with Agent Brief; link from Comments here. If none, close this issue to `closed/` with Answer “no new gaps.”

**Bindings:**

- Parent: [[issues-156-post-drain-next-wave-map]]
- Pattern: [[issues-147-demo-gap-followups]]
- Issue tracker: `docs/agents/issue-tracker.md`
- Do not “fix” product inside this ticket — file issues only

**Desired behavior:**

1. Run `examples/core-desk/scripts/dogfood.sh` (with built `odm` as prior dogfood issues) and `cargo test -p odm --test core_desk_full_tour`.
2. Classify failures: test flake vs product bug vs missing demo asset vs **expected** honesty smoke (e.g. `agent start` exit 1 while still sketch).
3. File only real product/docs bugs not already ticketed (check live + `closed/`).
4. Append list of new issue wikilinks under Comments (or explicit “none”).
5. Set status closed and move to `closed/` when done (even if zero new issues).

**Acceptance criteria:**

- [ ] Tour + gate executed; results logged under Comments
- [ ] New issues filed with `ready-for-agent` + Agent Brief, or explicit “no new gaps”
- [ ] No product implementation in this ticket itself
- [ ] This issue closed with Answer

**Out of scope:**

- Implementing filed bugs in the same session (optional follow-up)
- Human conversational `/qa` as a gate
- Treating expected `agent start` stub exit 1 as a new gap while start remains sketch
- Release cut, architecture refactors

## Comments

Minted from [[issues-156-post-drain-next-wave-map]] 2026-08-02.
