---
id: issues-156
title: "Post-drain next wave map"
description: "After empty frontier (154/155 closed): release hygiene, pick next product surface, dogfood/QA gap filing, then architecture deepen."
status: reviewing
issue-type: feature-request
severity: medium
tags:
  - planning
  - issue
  - wayfinder-map
  - release
  - product
  - qa
  - architecture
  - ready-for-agent
---

# Post-drain next wave map

## Destination

Frontier was empty after [[issues-154-update-readme-correct-info]] and [[issues-155-align-website-current-project]]. Clear four sequential workstreams so the next delivery wave is chosen, verified, and structurally ready:

1. **Ship / release hygiene** — decide what Unreleased CHANGELOG + docs state imply for main: cut a post-0.1 note/tag **or** explicitly keep rolling Unreleased; no silent drift between crate version, README status, website chip, and CHANGELOG.
2. **Next product map** — pick **one** deferred surface and open a child delivery map (not all at once): candidates from honesty docs — `agent start`, remote generate, marketplace, serve/MCP, interactive init. Grill scope before coding.
3. **Dogfood / QA gap filing** — run core-desk full tour (`examples/core-desk/scripts/dogfood.sh`, `cargo test -p odm --test core_desk_full_tour` if present) plus optional conversational QA; file real product/docs bugs as separate `ready-for-agent` issues (pattern of [[issues-147-demo-gap-followups]]). Close the QA child with “no new gaps” if clean.
4. **Architecture deepen** — run `/improve-codebase-architecture` (or equivalent review) after or beside product choice; file a thermo-nuclear-style child map only for high-leverage deepen tickets (file-size, seams, duplication) — no drive-by refactors.

## Notes

- **Authority:** empty issues Index frontier (2026-08-02), root README / website status honesty, CHANGELOG Unreleased, closed spine through [[issues-148-thermo-nuclear-structure-map]].
- **Standing prefs:**
  - One product surface per map; YAGNI on remote/marketplace until chosen.
  - Release decision is human-gated (tag/publish); agents may draft notes and version/docs alignment tickets only.
  - Dogfood must not invent failures into product changes without a filed issue.
  - Architecture map must preserve locked `--json` / exit contracts unless a child explicitly migrates them.
- **Suggested order:** 1 → 2 and 3 can parallel after 1’s decision is recorded; 4 after 2’s surface is chosen (or parallel if deepen is product-agnostic). Prefer child tickets over stuffing this map with implementation.

## Decisions so far

- Filed as umbrella map after afk-loop drain of 154/155 (verified PASS).
- Triage 2026-08-02: `ready-for-agent` with AFK defaults (see Agent Brief). Children not yet allocated — minting is the agent job.

### AFK defaults (triage)

- **Release:** stay rolling **0.1.0 + Unreleased** this wave — no crate version bump, no git tag, no publish.
- **Product surface:** **`agent start`** (packs + prompt landed; start is the remaining honesty stub). Other deferred surfaces stay out of this wave.
- **QA:** agent-only dogfood + full-tour gate (no human `/qa` required to close the QA child).
- **Architecture:** targeted deepen only (file-size / seams / duplication); not a full product redesign pass. Preserve locked `--json` and exit contracts.

## Fog / open questions

- _(cleared by AFK defaults above; maintainer may override before children execute)_

## Out of scope

- Implementing all deferred surfaces in one wave
- GitHub Issues (vault-only tracker)
- Coverage CI / product CI matrix (AGENTS.md)
- Website redesign beyond honesty already closed in 155

## Blocked by

None

## Agent Brief

> *This was generated by AI during triage.*

**Category:** enhancement  
**Summary:** Operationalize this umbrella: record AFK defaults on the map, mint four child issues, update the issues Index frontier, then stop — do not implement product code or close this map in the same session unless every child is already closed.

**Current behavior:**

- Only live issue is this wayfinder map; Index frontier points here with no children.
- Crates remain 0.1.0; CHANGELOG has a large `[Unreleased]`; README/website status honesty already call out post-0.1 lands and sketch items including `agent start`.
- Dogfood script and `core_desk_full_tour` gate exist (pattern of closed [[issues-147-demo-gap-followups]]).
- Prior structure deepen map [[issues-148-thermo-nuclear-structure-map]] and children 149–153 are closed.

**Desired behavior:**

1. **Decisions** — Ensure this map’s “Decisions so far / AFK defaults” match the triage defaults above (rolling Unreleased; product = `agent start`; agent-only QA; targeted architecture). If the maintainer already overrode any default in Comments, prefer the override.
2. **Mint exactly four children** under the vault issue tracker (allocate ids with high-water mark across live + `closed/`; never reuse). Each child gets frontmatter + body per issue-tracker rules, `status: open`, parent wikilink to this map, and tags as follows:
   - **Release hygiene** — `wayfinder` + `wayfinder-task` (or observation). Inventory crate version vs CHANGELOG vs README/website status chip; document “rolling Unreleased on 0.1.0” as the decision; fix only **honesty drift** (contradictory “shipped vs sketch” claims), not a version bump. No `git tag`, no crates.io/npm publish, no Cargo.toml version edits unless a later human ticket says so.
   - **Product map — `agent start`** — `wayfinder-map`. Destination: specify and deliver `odm agent start` only. Must grill/scope before implement tickets (exit behavior, project/wt binding, relation to `agent prompt` / packs). Explicit out of scope: remote generate, marketplace, serve/MCP, interactive init, other deferred worktree product.
   - **Dogfood / QA gap filing** — same shape as [[issues-147-demo-gap-followups]]: run core-desk dogfood script + `cargo test -p odm --test core_desk_full_tour`; file real product/docs bugs as separate `ready-for-agent` issues with Agent Briefs; close with “no new gaps” if clean. Do not “fix” product in the QA ticket itself.
   - **Architecture deepen** — `wayfinder-map` like [[issues-148-thermo-nuclear-structure-map]]. Scan for high-leverage deepen only (modules over file-size target, duplicated seams, thin-bin violations). File small child tasks only when leverage is clear. No drive-by refactors; no JSON/exit contract churn unless a child explicitly migrates a type.
3. **Index** — Refresh `docs/planning/issues/Index.md`: this map under Maps; frontier = open unblocked children (lowest id first) plus this map if still open; drop stale “children not yet filed” wording.
4. **Link back** — Append minted child wikilinks under this map’s Decisions so far; set each child’s `## Blocked by` only when truly blocked (release decision recorded here unblocks release + QA; product map should not block QA; architecture may run parallel if product-agnostic).
5. **Stop** — Do not implement `agent start`, do not cut a release, do not run the full deepen refactor in this ticket. Leave children `ready-for-agent` (or `needs-triage` only if a child still lacks a brief). This map stays `open` until children are done and an `## Answer` can be written in a later session.

**Key interfaces:**

- Vault issue tracker contract (`docs/agents/issue-tracker.md`) — filename `issues-<N>-<slug>.md`, single id sequence, frontmatter tags including wayfinder roles.
- Triage tags (`docs/agents/triage-labels.md`) — children that are AFK-executable get `ready-for-agent` + `## Agent Brief`.
- Honesty surfaces: CHANGELOG `[Unreleased]`, root README status line, website shipped-vs-sketch copy, reference deferred lists (`agent start` sketch).
- Verification tools: core-desk dogfood script; `core_desk_full_tour` integration test.
- Domain: Workspace, Project, worktree slot, agent pack, `agent prompt` (landed thin), `agent start` (sketch) — root `CONTEXT.md` + `docs/reference/env-gen-packs.md` / `cli.md`.

**Acceptance criteria:**

- [ ] This issue tagged `ready-for-agent` (not `needs-triage`); AFK defaults recorded under Decisions
- [ ] Four child issues filed with correct ids, wikilinks, and wayfinder tags
- [ ] Product child is scoped to **`agent start` only**
- [ ] Release child forbids tag/publish/version bump; allows honesty-only fixes
- [ ] QA child mirrors 147 run-and-file pattern; agent-only
- [ ] Architecture child is deepen-map shaped; no product verbs
- [ ] Index frontier lists the new open children
- [ ] This map Comments/Decisions list the four children; map remains open
- [ ] No product implementation, version bump, or git tag in the minting session

**Out of scope:**

- Implementing `agent start` or any other deferred product surface
- Cutting `v0.1.1` (or any tag) / publishing crates
- Human conversational `/qa` as a gate
- Full website redesign, coverage CI, GitHub Issues
- Closing this map before children complete

## Proposed Fix

1. ~~Triage this map~~ → `ready-for-agent` + Agent Brief.
2. File children per Agent Brief.
3. Execute frontier lowest-id first; close children; then Answer this map.

## Comments

### 2026-08-02 filed

User asked to create one issue covering four post-drain next moves: release hygiene, next product map, dogfood/QA, architecture deepen.

### 2026-08-02 triage

> *This was generated by AI during triage.*

- Category: enhancement (`feature-request`)
- State: `needs-triage` → `ready-for-agent`
- Redundancy: planning umbrella only; patterns [[issues-147-demo-gap-followups]], [[issues-148-thermo-nuclear-structure-map]] closed — not duplicates
- Prior rejection: `.out-of-scope/` empty
- Maintainer: skip grill; AFK defaults for release/product/QA/architecture fog; Agent Brief attached
