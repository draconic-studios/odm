---
id: issues-156
title: "Post-drain next wave map"
description: "After empty frontier (154/155 closed): release hygiene, pick next product surface, dogfood/QA gap filing, then architecture deepen."
status: reviewing
issue-type: feature-request
severity: medium
tags:
  - planning
  - issue
  - wayfinder-map
  - release
  - product
  - qa
  - architecture
---

# Post-drain next wave map

## Destination

Frontier was empty after [[issues-154-update-readme-correct-info]] and [[issues-155-align-website-current-project]]. Clear four sequential workstreams so the next delivery wave is chosen, verified, and structurally ready:

1. **Ship / release hygiene** — decide what Unreleased CHANGELOG + docs state imply for main: cut a post-0.1 note/tag **or** explicitly keep rolling Unreleased; no silent drift between crate version, README status, website chip, and CHANGELOG.
2. **Next product map** — pick **one** deferred surface and open a child delivery map (not all at once): candidates from honesty docs — `agent start`, remote generate, marketplace, serve/MCP, interactive init. Grill scope before coding.
3. **Dogfood / QA gap filing** — run core-desk full tour (`examples/core-desk/scripts/dogfood.sh`, `cargo test -p odm --test core_desk_full_tour` if present) plus optional conversational QA; file real product/docs bugs as separate `ready-for-agent` issues (pattern of [[issues-147-demo-gap-followups]]). Close the QA child with “no new gaps” if clean.
4. **Architecture deepen** — run `/improve-codebase-architecture` (or equivalent review) after or beside product choice; file a thermo-nuclear-style child map only for high-leverage deepen tickets (file-size, seams, duplication) — no drive-by refactors.

## Notes

- **Authority:** empty issues Index frontier (2026-08-02), root README / website status honesty, CHANGELOG Unreleased, closed spine through [[issues-148-thermo-nuclear-structure-map]].
- **Standing prefs:**
  - One product surface per map; YAGNI on remote/marketplace until chosen.
  - Release decision is human-gated (tag/publish); agents may draft notes and version/docs alignment tickets only.
  - Dogfood must not invent failures into product changes without a filed issue.
  - Architecture map must preserve locked `--json` / exit contracts unless a child explicitly migrates them.
- **Suggested order:** 1 → 2 and 3 can parallel after 1’s decision is recorded; 4 after 2’s surface is chosen (or parallel if deepen is product-agnostic). Prefer child tickets over stuffing this map with implementation.

## Decisions so far

- Filed as umbrella map after afk-loop drain of 154/155 (verified PASS).
- Triage 2026-08-02: `ready-for-agent` with AFK defaults (see Agent Brief).
- **Children minted 2026-08-02:**
  - [[issues-157-release-hygiene-rolling-unreleased]] — release hygiene (rolling Unreleased on 0.1.0; honesty-only)
  - [[issues-158-agent-start-map]] — product map (`agent start` only)
  - [[issues-159-post-drain-dogfood-qa-gaps]] — dogfood / QA gap filing (agent-only)
  - [[issues-160-post-drain-architecture-deepen-map]] — targeted architecture deepen
- Blocking: none of the four are blocked (release decision recorded here; QA does not wait on product; architecture product-agnostic).
- [[issues-157-release-hygiene-rolling-unreleased]] **closed** — inventory OK; rolling Unreleased on 0.1.0 confirmed; CHANGELOG one-line rolling note + 0.1.0 stub pointer; no tag/publish/version bump.
- [[issues-159-post-drain-dogfood-qa-gaps]] **closed** — dogfood OK + full_tour 1 passed; expected `agent start` exit 1; no new gaps.
- [[issues-158-agent-start-map]] **closed** — v1 one-shot `odm agent start` shipped (lib + CLI + docs/dogfood); start depth (runtime matrix / pack auto-apply / session) remains deferred.
- [[issues-160-post-drain-architecture-deepen-map]] **closed** — scan only; no deepen tickets this wave (all product modules ≤730 LOC; thin-bin Present/Ctx intact).

### AFK defaults (triage)

- **Release:** stay rolling **0.1.0 + Unreleased** this wave — no crate version bump, no git tag, no publish.
- **Product surface:** **`agent start`** (delivered as v1 one-shot; children 161–163 closed). Other deferred surfaces stay out of this wave.
- **QA:** agent-only dogfood + full-tour gate (no human `/qa` required to close the QA child).
- **Architecture:** targeted deepen only (file-size / seams / duplication); not a full product redesign pass. Preserve locked `--json` and exit contracts.

## Fog / open questions

- _(cleared by AFK defaults above; maintainer may override before children execute)_

## Out of scope

- Implementing all deferred surfaces in one wave
- GitHub Issues (vault-only tracker)
- Coverage CI / product CI matrix (AGENTS.md)
- Website redesign beyond honesty already closed in 155

## Blocked by

None

## Agent Brief

> *This was generated by AI during triage.*

**Category:** enhancement  
**Summary:** Close out this umbrella map — children and wave outcomes are done; write Answer, close, refresh Index.

**Current behavior:**

- All four children closed: [[issues-157-release-hygiene-rolling-unreleased]], [[issues-158-agent-start-map]], [[issues-159-post-drain-dogfood-qa-gaps]], [[issues-160-post-drain-architecture-deepen-map]].
- Agent start delivery children 161–163 closed; `odm agent start` v1 one-shot is shipped.
- Mint-phase Agent Brief acceptance criteria are all checked; map body still `status: open` with no `## Answer`.
- Index frontier still lists this map as “Answer/close map next.”

**Desired behavior:**

1. Append `## Answer` summarizing the wave (rolling Unreleased kept; product surface `agent start` v1 landed; QA clean; architecture scan no deepen tickets).
2. Set `status: closed`, drop `ready-for-agent` if desired, move file to `docs/planning/issues/closed/` (same filename).
3. Refresh `docs/planning/issues/Index.md`: remove this map from Maps + Frontier; add under Closed maps; leave 164/165 as live frontier.
4. Do not reopen product work, version bumps, or tags from this ticket.

**Key interfaces:**

- Vault issue tracker close rules (`docs/agents/issue-tracker.md`) — status + move to `closed/`.
- Parent/child wikilinks already on Decisions so far — keep stable.

**Acceptance criteria:**

- [ ] `## Answer` present with one-paragraph wave outcome + child wikilinks
- [ ] Issue `status: closed` and file under `docs/planning/issues/closed/`
- [ ] Index no longer lists this map as live frontier; Closed maps includes it
- [ ] No product code, Cargo version edit, git tag, or publish in the close-out session

**Out of scope:**

- Re-running dogfood or architecture scan
- Filing new product maps (that is separate work, e.g. [[issues-165-multiplatform-github-releases-curl-install]])
- Docs/site residual audit ([[issues-164-update-docs-and-website]])

## Proposed Fix

1. ~~Triage / mint children / execute wave~~ done.
2. Agent: Answer + close + Index (this brief).

## Comments

### 2026-08-02 filed

User asked to create one issue covering four post-drain next moves: release hygiene, next product map, dogfood/QA, architecture deepen.

### 2026-08-02 triage

> *This was generated by AI during triage.*

- Category: enhancement (`feature-request`)
- State: `needs-triage` → `ready-for-agent`
- Redundancy: planning umbrella only; patterns [[issues-147-demo-gap-followups]], [[issues-148-thermo-nuclear-structure-map]] closed — not duplicates
- Prior rejection: `.out-of-scope/` empty
- Maintainer: skip grill; AFK defaults for release/product/QA/architecture fog; Agent Brief attached

### 2026-08-02 children minted

AFK minting session: claimed briefly, filed 157–160, refreshed Index frontier, left this map `open` (dropped `ready-for-agent` — mint job done; children hold the frontier). No product code, version bump, or git tag.

### 2026-08-02 triage (close-out)

> *This was generated by AI during triage.*

- Wave complete (157–160 + 161–163 closed). Re-tagged `ready-for-agent` with close-out Agent Brief only.
