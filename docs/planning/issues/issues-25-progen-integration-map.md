---
id: issues-25
title: "Progen integration map"
description: "Wayfinder map: phase 3 — odm-progen façade, federation/scope, find/context."
status: open
issue-type: feature-request
tags:
  - planning
  - issue
  - wayfinder-map
  - ready-for-agent
---

# Progen integration map

## Destination

Phase **3. Progen integration** per `docs/reference/phased-delivery.md`: Progen stores behind an `odm-progen` façade; store verbs under `odm progen …`; federation and scope per `docs/reference/progen.md`; ODM-side index/cache under `.odm/progen/<name>/`; top-level `find` / `context` honor scope rules. Each Progen path is an **Obsidian-compatible vault** (plain Markdown + YAML frontmatter + `[[wikilinks]]`) so Obsidian and obsidian-cli can open the same tree.

## Notes

- **Domain:** root `CONTEXT.md`. Design package closed — do not reopen fundamentals without an explicit decision change.
- **Authority:** `phased-delivery.md` (phase 3), `progen.md`, `cli.md`, `architecture.md`, `config.md`.
- **Execution override:** Resolving a ticket means the decision is recorded **and** the relevant code, tests, and/or example artifacts land — not abstract answers only (same as Implement core).
- **Standing prefs (charted 2026-08-01):**
  - **Engine day one:** in-repo `odm-progen` vault engine (Markdown-on-disk source of truth). No external path-dep on progenitor crates required for v1 phase 3; façade shape stays swap-ready for upstream `progen-ops` later.
  - **Obsidian contract:** store root = vault root; content is plain `.md`; engine index **only** under `.odm/progen/<name>/` (never required inside the vault); optional minimal `.obsidian/` scaffold on local add so Obsidian recognizes the folder; leave existing `.obsidian/` untouched.
  - **Crates:** add `crates/odm-progen`; bin depends on it; `odm-core` keeps lifecycle/config.
  - **CLI cut:** `progen` list/add/rm/info + get/ls/reindex/doctor; top-level `find` + `context`; globals `--progen` / `--progen-group`.
  - **Writes:** single-Progen only (`--progen` or sole configured).
  - **Reads under `odm progen`:** single-root; federated at `find` / `context`.
  - **Dogfood:** path-only local vault under example Workspace (offline).
  - **Proof:** unit tests + integration tests driving `odm` binary.
- **Skills:** `/tdd` when landing code; `/domain-modeling` if terms shift.

## Decisions so far

- [[issues-28-progen-engine-and-obsidian-contract]] — **done** — in-repo vault engine; Obsidian-compatible MD vault; index under `.odm/progen/<name>/`.
- [[issues-29-progen-slice-order-and-acceptance]] — **done** — slices 1–6 + phase gate checklist.
- **2026-08-01 implement land:** `odm-progen` crate; lifecycle `progen list|add|rm|info`; store `get|ls|reindex|doctor`; top-level `find` + `context`; core-desk `progens/notes` vault; integration tests green. Phase gate checklist complete — human may close this map.

## Not yet specified

- Full upstream progen node-kind CRUD surface (`task|issue|idea|decision|doc set`) — deferred past phase gate
- `progen watch` / live index daemon
- Wiring path/git dep on external progenitor crates (optional later swap)
- Multi-vault rank fusion beyond config-order concat

## Out of scope

- Actions pipelines (phase 4)
- Cross-store wikilinks / federated id graph
- Upstreaming multi-root into progen crates
- Sketch productization (worktrees, agent packs, graph product, env)
- `serve` / MCP
- Reopening design fundamentals without explicit decision change

## Blocked by

- [[issues-14-implement-core-map]] (closed — core done)

## Comments

> *This was generated by AI during triage.*

Charted with maintainer direction: choose best options + fully implement Obsidian-compatible progen management in-repo.
