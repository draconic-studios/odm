---
description: Run one AFK swarm pipeline cycle (orchestrator)
agent: swarm-orchestrator
---

Execute exactly one swarm pipeline cycle.

Follow @.opencode/prompts/swarm-iteration.md and your swarm-orchestrator instructions.

End with `SWARM_EXIT:0`, `SWARM_EXIT:2`, or `SWARM_EXIT:3`.
